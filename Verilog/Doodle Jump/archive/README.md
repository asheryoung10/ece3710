Active sources now live under `rtl/`, `sim/`, `mem/`, and `filelists/`.

`archive/bak/` contains the pre-refactor backup files that used to sit beside active sources.
`archive/generated/` contains stray generated artifacts that should not be part of the active build.
